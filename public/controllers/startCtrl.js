'use strict';

// Dependencies
angular.module('myApp')
    .controller('startCtrl', ['$rootScope','$scope', '$log', '$state',
        function ($rootScope, $scope, $log, $state) {

    	/*
    	$scope.goWatson = function() {
    		$state.go('top.watson', {}, { reload: true });
    	};*/

        }])